<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.talkademy_phase4">

    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Talkademy_Phase4">
        <activity android:name=".phase4.MainActivity" />
        <activity android:name=".phase5.StartActivity" />
        <activity android:name=".phase5.DestinationActivity" />
        <activity android:name=".phase5.TestLifeCycleActivity" />
        <activity android:name=".phase6.Phase6Activity" />
        <activity android:name=".phase7.activities.CountriesListActivity" />
        <activity android:name=".phase7.activities.CountryDetailActivity" />
        <activity android:name=".phase9.ContactActivity">

        </activity>
        <activity android:name=".phase10.ui.Phase10Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>