<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.talkademy_phase4">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Talkademy_Phase4">

        <activity android:name=".phase5.TestLifeCycleActivity"></activity>
        <activity android:name=".phase4.MainActivity"></activity>
        <activity android:name=".phase5.StartActivity"></activity>
        <activity android:name=".phase5.DestinationActivity" />
    </application>

</manifest>